query getPrivacyPage($relativePath: String!) {
  getPrivacyDocument(relativePath: $relativePath) {
    id
    data {
      seo_data {
        meta_title
        meta_description
        meta_canonical
      }
      hero {
        hero_heading
        hero_description
        hero_button_text
      }
      body
    }
    values
    dataJSON
  }
}
